// SPDX-License-Identifier: MIT

// BabyMan Presents https://babyfuse.money
// $BabyFUSE proposes an innovative feature in its contract.
//
// DIVIDEND YIELD PAID IN FUSE! With the auto-claim feature,
// simply hold $BabyFUSE and you'll receive FUSE automatically in your wallet.
//
// Hold Baby FUSE and get rewarded in FUSE on every transaction!
/*
  _______    _______   _______    ___ ___       _______   ___ ___   _______   _______
 |   _   \  |   _   | |   _   \  |   Y   |     |   _   | |   Y   | |   _   | |   _   |
 |.  1   /  |.  1   | |.  1   /  |   1   |     |.  1___| |.  |   | |   1___| |.  1___|
 |.  _   \  |.  _   | |.  _   \   \_   _/      |.  __)   |.  |   | |____   | |.  __)_
 |:  1    \ |:  |   | |:  1    \   |:  |       |:  |     |:  1   | |:  1   | |:  1   |
 |::.. .  / |::.|:. | |::.. .  /   |::.|       |::.|     |::.. . | |::.. . | |::.. . |
 `-------'  `--- ---' `-------'    `---'       `---'     `-------' `-------' `-------'
;cxddxoxKKKKKKKKOxONMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM
:'.'...oNMMMMMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMNkxKKKKKKKKKKK0dkKKKKKKKKKKKKKKKKKKKKKKK0Okxolc:;;;..       .,xNMMMMMMMMMMMMMMNK0000KNWMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMKxOKKKKKKKKKKKKO0KKKKKKKKKKKKKK0Okxdoc:;'....:c'.lxl;,.    ..dNMMMMMMMMMMMMMMNdlxxxdcoXMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM0x0KKKKKKKKKKKKKKKKKK0Oxoc::::;,'...   ......:lldo;;oo'  ..;dx0WMMMMMMMMMMMMM0:l0000lcKMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM0x0KKKKKKKKKK0Okxolc:;,,.             .;dkOdc,.':;..,,.,loxOKkxXMMMMMMMMMMMMM0:cO00x;oNMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMKxOK0Okxdlc:;;'...;c'.cxl;,,.        .:kKK0Okdlloollo;:kKKKKK0dOWMMMMMMMMMMMMWk;lOx;cKMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMNx::;;;;;:cldd:'..:lcodccdkdc:,.   ..ckKK00OOdd0KKK00xx0KKKKK0dOMMMMMMMMMMMMMMK::x::0MMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWKOl'cdkO00KKKKK0Okl;..;:lxl;';oc...';d0KKOxk0kdx00OO0KKKKKKKK0kONMMMMMMMMMMMMMM0:lx;dWMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMXd;',l0KKKKKKKKKKKKK0d:'.,c:,,.';;:dO0KKKK0kddooooolloox0KKKKOk0NMMMMMMMMMMMMMMM0cdxckMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM0dxdokKKKKKKKKKKKKKK0kxxkO000Oxolx0KKKK0xdollllloooooocd00kk0XMMMMMMMMMMMMMMMMMO:xxcOMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWXxd0xd0KKKKKKKKKKKKKKKKKKKKKKKKKKOxkKKKxlloooolcccllc:c:cxO0NMMMMMMMMMMMMMMMMMMWx:xoc0MMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWX0OOdoOkdOKOkOKKKKKKKKKKKKKKKKKKKKKKKKKKK0ocodo:;cooool;';o0NMMMMMMMMMMMMMMMMMMMMMWo;xlcKMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMNOk0XX0llkkxxxk00kxOKKKKKKKKKKKKKKKKKKKKKKKKOdlc,..::;,,,,cOWMMMMMMMMMMMMMMMMMMMMMMMWo;dclNMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMWXkkKXXXKdlddkO0KOdllllxO0KKKKKKKKKKKKKKKKK0Okxdoc,;:::::cllo0WMMMMMMMMMMMMMMMMMMMMMMMWo:d:dWMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMWKkOXXXXXXxoxdkkkkkxoool:coxkkkkOOOOOOOOkxdlldddkK0ocldO000KKkoxXMMMMMMMMMMMMMMMMMMMMMMWdco:kMMMMMM
:ldooOXXXXXXXXXXOoo0WMMMMMMMMMMMMMMMMXl:xxlkWMMMMM
MMMMMMMMMN0OO00K0OKKd:;l0XXXXXXXXXXXkoooxKXXXXXXXKOdlc:;;;:cloxk0KXXXXXXXX0l:oolOXXXXXXXXXXXKkcdXMMMMMMMMMMMMMMXl,cddclKWMMMM
MMMMMMMMNdl0K00KklxOc'';xKXXXXXXXXXXkocoKXXXXXKkc,..         ...':d0XXXXXXXOcloo0XXXXXXXXXXXXOl:dXMMMMMMMMMMMMNd,lkkkkllKMMMM
MMMMMMMWKl:odccxo;;::lc'.;okOKKXXXKKxlcckXXXKx:..    ...''..      .,o0XXXXKklolo0XXXXXXXXXX0kxOOoxNWMMMMMMMMMKo:coxOkoc:oXMMM
MMMMMW0doxO0OxddxkOkk0Kkl;,:odxdollxdldccOX0c..  ..,:ldkOOxoc;,'... .;kXXKxlodldKXXKXXXXXOxxOKK0ddkkkOKNMMMMNd,ldkKX0doo:kWMM
MMMW0oox0KKKKK00K0O0KKKKK0dclk0Oxooxoldlo00:.  .:okO0000000000OOxo,.  'xXOccddoxKXdcdO0xlokKKKKKKKK0Oxxxk0XKx::ddkKKkool,lXWM
MWKdlxKKKKKKK0o;c:,lOKKKKKK0dccxKkkOoollOKl.   .cdkO0000000000Okdl,.  .;OKo:odoxKKdlkOOklcd0KKKKKKKKKKK0xloddxxodKKKxcododxx0
WkcoOKKKKKKKx;......;d0KKKKKKOlcloOklolcxk;    ..':loodkkkxoolll:'.    .dXklodcdXKxOMMMMNOolxKKKKKKKKKKkdk0KK0kod0KKk:;dO0Kxk
dcxKKKKKKKKk,.,':o,:xOKKKKKKKKKx,;Oxcodlcc'    'oO00kxdoooodkO0K0k;.   .oKxloocdXkdXMMMMMMWOlo0KKKKKKKKockK0dlodxOKKxldxk0KO0
:kKKKKKKKKKOc.'.';.,d0KKKKKKKKKKx:llcddoll,    .;ldkOKKKKKKK0kxdl:..   .ldlodocoOx0WMMMMMMMMXxodkO00000d,:kd';ooxKXOlcx0KK0kK
kKKKKKKKKKKK0xc..,...;d0KKKKKKKKKd,'ldooO0c.   .,cooloodddoooooooc'.   ;o:lddo,;dKWMMMMMMMMMMWXOOO000OOxc;xd,:dokKKkl:o0K0kOW
KKKKKKKKKKKKKK0c,o:;c.'xKKKKKKKKKO;.locxKXk;.  'oOKK0OxdddxO0KKK0x;.  .d0ocodl,,lOXWMMMMMMMMMMMMMMMMMMMMN00d,:od0KKxll:lk0KNM
KKKKKKKKKKKOl:c,'l;';.'xKKKKKKKKK0c,odlcdKXk;. ..';cok0KKK0kdl:;'.. .'dKKdcodclkkxdxxkKWMMMMMMMMMMMMMMMMMMWk;coxKK0xoo:lKWMMM
KKKKKKKKKKKOo;'......:xKKKKKKKKKK0:,ldocoKXX0o,.    ..';;;,..     ..cOXOdlodocdXXXXKOxdxKWMMMMMMMMMMMMMMMMMXl:lxKKOddc:OMMMMM
kKKKKKKKKKKKKKOc;lc;xKKKKKKKK0k0Kx,,ldllOXXXXX0d;...           ..,lOKXOlldddllkXXXXXXXKkodKWNKK00OOOKNWMMMMWO;,okkdc:lOWMMMMM
:d0KKKKKKKKKKKK0OK00KKKKKKK0xxO0xclccdclOKKXXXXXKOxl:;,''',,;codkKXXXXO::oddldKXXXXXXXXX0l:odddodoolodk0XWMMNx:clcc:oXMMMMMMM
klcdk0KK0kxkOKKKKKKKKKKKOxl;coo:;dKkcclclooood0XXXXXKKK0000KXXXXXXXXXXKd:ldloO00XXXXXXXKkoloddddoooooooolkNMMWX0000KNMMMMMMMM
MNOdollool,';clloddoolc:;,,,;;;;:cx0Oo::lodol;lOOkxdoooxOKXXK00OkxxO00kdloodO0OKXXXXXKOdooooooooooooooodolkWMMMMMMMMMMMMMMMMM
MMMMWNKK00Ooclcc:::;:::cloooodoool:lkKOdlllllccclcloolc:;;cclooolc:;,,:ldxOKXKXXXXXX0doddoodoooooooooooodlxNMMMMMMMMMMMMMMMMM
MMMMMMMMMMMOlooooodoooooooooooododoc:dKXKKOkkkkxxxxxxdddlcccoxxxxxxxdxkOKKXXXXXXXXKOdodxdoooooooooddoooodlxNMMMMMMMMMMMMMMMMM
MMMMMMMMMMMKoldoooooooooooooooooododl:lOK0o;o0XXXXXXXXXXXXXXXXXXXXXXXXXXOolkKXXXXKkooddddooooooooooooooodokWMMMMMMMMMMMMMMMMM
MMMMMMMMMMMWkclooooooooooooooooooodooo:c00l.;d0XXXXXXXXXXXXXXXXXXXXXXX0kdc';xkOKKkooddooooooooooooooooodoxXMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMWkcloooooooooooooooooodoodl;codOxoodkO00KKKKKKKKK00OOkkxkkxxO00dc:okdlodooooooooooooooooooooxXMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMWOlcooooooooooooooooooodoo::kNWMWX0kkxkkkkxkkkkkkkkkO0KXNWWWWMMXkd:,ldooooooooooooooooooooxXMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMXxccodoooooooddooooooddo:lXMMMMMMMMMMWWWWWWWWMMMMMMMMMMMMMMMMMW0c:dooooooooooooooooooodONMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMW0l,:loddooodooooooodolcxXMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMW0ccdooooooooooodooooodOXMMMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMWX0kxdollllooooooooolloKMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWXd:odooodooooooddoodOXWMMMMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMWXXXNWNKOkxxxxxxxxxO0XWMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWXxc:lddoooodddolcd0NMMMMMMMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMWWWWWWMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMXdccllloooolldOXMMMMMMMMMMMMMMMMMMMMMMMMMMMMM
MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMWNOxddxkO0KXWMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM
  _______    _______   _______    ___ ___       _______   ___ ___   _______   _______
 |   _   \  |   _   | |   _   \  |   Y   |     |   _   | |   Y   | |   _   | |   _   |
 |.  1   /  |.  1   | |.  1   /  |   1   |     |.  1___| |.  |   | |   1___| |.  1___|
 |.  _   \  |.  _   | |.  _   \   \_   _/      |.  __)   |.  |   | |____   | |.  __)_
 |:  1    \ |:  |   | |:  1    \   |:  |       |:  |     |:  1   | |:  1   | |:  1   |
 |::.. .  / |::.|:. | |::.. .  /   |::.|       |::.|     |::.. . | |::.. . | |::.. . |
 `-------'  `--- ---' `-------'    `---'       `---'     `-------' `-------' `-------'

Rewards Token FUSE 0x0BE9e53fd7EDaC9F859882AfdDa116645287C629

Tokenomics

15% Buy tax

16% Sell tax

7% Rewards

5% Marketing

3% Liquidity Pool


*/
// 📱 Telegram: https://t.me/babyfusemoney
// 🌎 Website: https://www.babyfuse.money/
// 🌎 Email: babyfusemoney@gmail.com
// 🌐 Twitter: @babyfusemoney
// 🌐 GITHUB REPO https://github.com/BabyFUSE/Baby-Fuse-Contracts

pragma solidity ^0.6.2;

import "./DividendPayingToken.sol";
import "./SafeMath.sol";
import "./IterableMapping.sol";
import "./Ownable.sol";
import "./IUniswapV2Pair.sol";
import "./IUniswapV2Factory.sol";
import "./IUniswapV2Router.sol";


contract BABYFUSE is ERC20, Ownable {
    using SafeMath for uint256;

    IUniswapV2Router02 public uniswapV2Router;
    address public  uniswapV2Pair;

    bool private swapping;

    BABYCAKEDividendTracker public dividendTracker;

    address public deadWallet = 0x000000000000000000000000000000000000dEaD;

    address public immutable CAKE = address(0x0BE9e53fd7EDaC9F859882AfdDa116645287C629); //FUSE

    uint256 public swapTokensAtAmount = 2000000 * (10**18);

    mapping(address => bool) public _isBlacklisted;

    uint256 public CAKERewardsFee = 7;
    uint256 public liquidityFee = 3;
    uint256 public marketingFee = 5;
    uint256 public totalFees = CAKERewardsFee.add(liquidityFee).add(marketingFee);

    address public _marketingWalletAddress = 0xe36b2472A51d0c71720f88F68C1360fF6bCA6323;


    // use by default 300,000 gas to process auto-claiming dividends
    uint256 public gasForProcessing = 350000;

     // exlcude from fees and max transaction amount
    mapping (address => bool) private _isExcludedFromFees;


    // store addresses that a automatic market maker pairs. Any transfer *to* these addresses
    // could be subject to a maximum transfer amount
    mapping (address => bool) public automatedMarketMakerPairs;

    event UpdateDividendTracker(address indexed newAddress, address indexed oldAddress);

    event UpdateUniswapV2Router(address indexed newAddress, address indexed oldAddress);

    event ExcludeFromFees(address indexed account, bool isExcluded);
    event ExcludeMultipleAccountsFromFees(address[] accounts, bool isExcluded);

    event SetAutomatedMarketMakerPair(address indexed pair, bool indexed value);

    event LiquidityWalletUpdated(address indexed newLiquidityWallet, address indexed oldLiquidityWallet);

    event GasForProcessingUpdated(uint256 indexed newValue, uint256 indexed oldValue);

    event SwapAndLiquify(
        uint256 tokensSwapped,
        uint256 ethReceived,
        uint256 tokensIntoLiqudity
    );

    event SendDividends(
    	uint256 tokensSwapped,
    	uint256 amount
    );

    event ProcessedDividendTracker(
    	uint256 iterations,
    	uint256 claims,
        uint256 lastProcessedIndex,
    	bool indexed automatic,
    	uint256 gas,
    	address indexed processor
    );

    constructor() public ERC20("BABY FUSE", "BABYFUSE") {

    	dividendTracker = new BABYCAKEDividendTracker();


    	IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(0x6d6cF5767651F68EFb3EC1d9d7d1d93f9A41Eedc);
         // Create a uniswap pair for this new token
        address _uniswapV2Pair = IUniswapV2Factory(_uniswapV2Router.factory())
            .createPair(address(this), _uniswapV2Router.WETH());

        uniswapV2Router = _uniswapV2Router;
        uniswapV2Pair = _uniswapV2Pair;

        _setAutomatedMarketMakerPair(_uniswapV2Pair, true);

        // exclude from receiving dividends
        dividendTracker.excludeFromDividends(address(dividendTracker));
        dividendTracker.excludeFromDividends(address(this));
        dividendTracker.excludeFromDividends(owner());
        dividendTracker.excludeFromDividends(deadWallet);
        dividendTracker.excludeFromDividends(address(_uniswapV2Router));

        // exclude from paying fees or having max transaction amount
        excludeFromFees(owner(), true);
        excludeFromFees(_marketingWalletAddress, true);
        excludeFromFees(address(this), true);

        /*
            _mint is an internal function in ERC20.sol that is only called here,
            and CANNOT be called ever again
        */
        _mint(owner(), 100000000000 * (10**18));
    }

    receive() external payable {

  	}

    function updateDividendTracker(address newAddress) public onlyOwner {
        require(newAddress != address(dividendTracker), "BABYFUSE: The dividend tracker already has that address");

        BABYCAKEDividendTracker newDividendTracker = BABYCAKEDividendTracker(payable(newAddress));

        require(newDividendTracker.owner() == address(this), "BABYFUSE: The new dividend tracker must be owned by the BABYFUSE token contract");

        newDividendTracker.excludeFromDividends(address(newDividendTracker));
        newDividendTracker.excludeFromDividends(address(this));
        newDividendTracker.excludeFromDividends(owner());
        newDividendTracker.excludeFromDividends(address(uniswapV2Router));

        emit UpdateDividendTracker(newAddress, address(dividendTracker));

        dividendTracker = newDividendTracker;
    }

    function updateUniswapV2Router(address newAddress) public onlyOwner {
        require(newAddress != address(uniswapV2Router), "BABYFUSE: The router already has that address");
        emit UpdateUniswapV2Router(newAddress, address(uniswapV2Router));
        uniswapV2Router = IUniswapV2Router02(newAddress);
        address _uniswapV2Pair = IUniswapV2Factory(uniswapV2Router.factory())
            .createPair(address(this), uniswapV2Router.WETH());
        uniswapV2Pair = _uniswapV2Pair;
    }

    function excludeFromFees(address account, bool excluded) public onlyOwner {
        require(_isExcludedFromFees[account] != excluded, "BABYFUSE: Account is already the value of 'excluded'");
        _isExcludedFromFees[account] = excluded;

        emit ExcludeFromFees(account, excluded);
    }

    function excludeMultipleAccountsFromFees(address[] calldata accounts, bool excluded) public onlyOwner {
        for(uint256 i = 0; i < accounts.length; i++) {
            _isExcludedFromFees[accounts[i]] = excluded;
        }

        emit ExcludeMultipleAccountsFromFees(accounts, excluded);
    }

    function setMarketingWallet(address payable wallet) external onlyOwner{
        _marketingWalletAddress = wallet;
    }

    function setCAKERewardsFee(uint256 value) external onlyOwner{
        CAKERewardsFee = value;
        totalFees = CAKERewardsFee.add(liquidityFee).add(marketingFee);
    }

    function setLiquiditFee(uint256 value) external onlyOwner{
        liquidityFee = value;
        totalFees = CAKERewardsFee.add(liquidityFee).add(marketingFee);
    }

    function setMarketingFee(uint256 value) external onlyOwner{
        marketingFee = value;
        totalFees = CAKERewardsFee.add(liquidityFee).add(marketingFee);

    }


    function setAutomatedMarketMakerPair(address pair, bool value) public onlyOwner {
        require(pair != uniswapV2Pair, "BABYFUSE: The NextGEN pair cannot be removed from automatedMarketMakerPairs");

        _setAutomatedMarketMakerPair(pair, value);
    }

    function blacklistAddress(address account, bool value) external onlyOwner{
        _isBlacklisted[account] = value;
    }


    function _setAutomatedMarketMakerPair(address pair, bool value) private {
        require(automatedMarketMakerPairs[pair] != value, "BABYFUSE: Automated market maker pair is already set to that value");
        automatedMarketMakerPairs[pair] = value;

        if(value) {
            dividendTracker.excludeFromDividends(pair);
        }

        emit SetAutomatedMarketMakerPair(pair, value);
    }


    function updateGasForProcessing(uint256 newValue) public onlyOwner {
        require(newValue >= 200000 && newValue <= 500000, "BABYFUSE: gasForProcessing must be between 200,000 and 500,000");
        require(newValue != gasForProcessing, "BABYFUSE: Cannot update gasForProcessing to same value");
        emit GasForProcessingUpdated(newValue, gasForProcessing);
        gasForProcessing = newValue;
    }

    function updateClaimWait(uint256 claimWait) external onlyOwner {
        dividendTracker.updateClaimWait(claimWait);
    }

    function getClaimWait() external view returns(uint256) {
        return dividendTracker.claimWait();
    }

    function getTotalDividendsDistributed() external view returns (uint256) {
        return dividendTracker.totalDividendsDistributed();
    }

    function isExcludedFromFees(address account) public view returns(bool) {
        return _isExcludedFromFees[account];
    }

    function withdrawableDividendOf(address account) public view returns(uint256) {
    	return dividendTracker.withdrawableDividendOf(account);
  	}

	function dividendTokenBalanceOf(address account) public view returns (uint256) {
		return dividendTracker.balanceOf(account);
	}

	function excludeFromDividends(address account) external onlyOwner{
	    dividendTracker.excludeFromDividends(account);
	}

    function getAccountDividendsInfo(address account)
        external view returns (
            address,
            int256,
            int256,
            uint256,
            uint256,
            uint256,
            uint256,
            uint256) {
        return dividendTracker.getAccount(account);
    }

	function getAccountDividendsInfoAtIndex(uint256 index)
        external view returns (
            address,
            int256,
            int256,
            uint256,
            uint256,
            uint256,
            uint256,
            uint256) {
    	return dividendTracker.getAccountAtIndex(index);
    }

	function processDividendTracker(uint256 gas) external {
		(uint256 iterations, uint256 claims, uint256 lastProcessedIndex) = dividendTracker.process(gas);
		emit ProcessedDividendTracker(iterations, claims, lastProcessedIndex, false, gas, tx.origin);
    }

    function claim() external {
		dividendTracker.processAccount(msg.sender, false);
    }

    function getLastProcessedIndex() external view returns(uint256) {
    	return dividendTracker.getLastProcessedIndex();
    }

    function getNumberOfDividendTokenHolders() external view returns(uint256) {
        return dividendTracker.getNumberOfTokenHolders();
    }


    function _transfer(
        address from,
        address to,
        uint256 amount
    ) internal override {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");
        require(!_isBlacklisted[from] && !_isBlacklisted[to], 'Blacklisted address');

        if(amount == 0) {
            super._transfer(from, to, 0);
            return;
        }

		uint256 contractTokenBalance = balanceOf(address(this));

        bool canSwap = contractTokenBalance >= swapTokensAtAmount;

        if( canSwap &&
            !swapping &&
            !automatedMarketMakerPairs[from] &&
            from != owner() &&
            to != owner()
        ) {
            swapping = true;

            uint256 marketingTokens = contractTokenBalance.mul(marketingFee).div(totalFees);
            swapAndSendToFee(marketingTokens);

            uint256 swapTokens = contractTokenBalance.mul(liquidityFee).div(totalFees);
            swapAndLiquify(swapTokens);

            uint256 sellTokens = balanceOf(address(this));
            swapAndSendDividends(sellTokens);

            swapping = false;
        }


        bool takeFee = !swapping;

        // if any account belongs to _isExcludedFromFee account then remove the fee
        if(_isExcludedFromFees[from] || _isExcludedFromFees[to]) {
            takeFee = false;
        }

        if(takeFee) {
        	uint256 fees = amount.mul(totalFees).div(100);
        	if(automatedMarketMakerPairs[to]){
        	    fees += amount.mul(1).div(100);
        	}
        	amount = amount.sub(fees);

            super._transfer(from, address(this), fees);
        }

        super._transfer(from, to, amount);

        try dividendTracker.setBalance(payable(from), balanceOf(from)) {} catch {}
        try dividendTracker.setBalance(payable(to), balanceOf(to)) {} catch {}

        if(!swapping) {
	    	uint256 gas = gasForProcessing;

	    	try dividendTracker.process(gas) returns (uint256 iterations, uint256 claims, uint256 lastProcessedIndex) {
	    		emit ProcessedDividendTracker(iterations, claims, lastProcessedIndex, true, gas, tx.origin);
	    	}
	    	catch {

	    	}
        }
    }

    function swapAndSendToFee(uint256 tokens) private  {

        uint256 initialCAKEBalance = IERC20(CAKE).balanceOf(address(this));

        swapTokensForCake(tokens);
        uint256 newBalance = (IERC20(CAKE).balanceOf(address(this))).sub(initialCAKEBalance);
        IERC20(CAKE).transfer(_marketingWalletAddress, newBalance);
    }

    function swapAndLiquify(uint256 tokens) private {
       // split the contract balance into halves
        uint256 half = tokens.div(2);
        uint256 otherHalf = tokens.sub(half);

        // capture the contract's current ETH balance.
        // this is so that we can capture exactly the amount of ETH that the
        // swap creates, and not make the liquidity event include any ETH that
        // has been manually sent to the contract
        uint256 initialBalance = address(this).balance;

        // swap tokens for ETH
        swapTokensForEth(half); // <- this breaks the ETH -> HATE swap when swap+liquify is triggered

        // how much ETH did we just swap into?
        uint256 newBalance = address(this).balance.sub(initialBalance);

        // add liquidity to uniswap
        addLiquidity(otherHalf, newBalance);

        emit SwapAndLiquify(half, newBalance, otherHalf);
    }


    function swapTokensForEth(uint256 tokenAmount) private {


        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // make the swap
        uniswapV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            address(this),
            block.timestamp
        );

    }

    function swapTokensForCake(uint256 tokenAmount) private {

        address[] memory path = new address[](3);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();
        path[2] = CAKE;

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // make the swap
        uniswapV2Router.swapExactTokensForTokensSupportingFeeOnTransferTokens(
            tokenAmount,
            0,
            path,
            address(this),
            block.timestamp
        );
    }

    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {

        // approve token transfer to cover all possible scenarios
        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // add the liquidity
        uniswapV2Router.addLiquidityETH{value: ethAmount}(
            address(this),
            tokenAmount,
            0, // slippage is unavoidable
            0, // slippage is unavoidable
            address(0),
            block.timestamp
        );

    }

    function swapAndSendDividends(uint256 tokens) private{
        swapTokensForCake(tokens);
        uint256 dividends = IERC20(CAKE).balanceOf(address(this));
        bool success = IERC20(CAKE).transfer(address(dividendTracker), dividends);

        if (success) {
            dividendTracker.distributeCAKEDividends(dividends);
            emit SendDividends(tokens, dividends);
        }
    }
}

contract BABYCAKEDividendTracker is Ownable, DividendPayingToken {
    using SafeMath for uint256;
    using SafeMathInt for int256;
    using IterableMapping for IterableMapping.Map;

    IterableMapping.Map private tokenHoldersMap;
    uint256 public lastProcessedIndex;

    mapping (address => bool) public excludedFromDividends;

    mapping (address => uint256) public lastClaimTimes;

    uint256 public claimWait;
    uint256 public immutable minimumTokenBalanceForDividends;

    event ExcludeFromDividends(address indexed account);
    event ClaimWaitUpdated(uint256 indexed newValue, uint256 indexed oldValue);

    event Claim(address indexed account, uint256 amount, bool indexed automatic);

    constructor() public DividendPayingToken("BABYFUSE_Dividen_Tracker", "BABYFUSE_Dividend_Tracker") {
    	claimWait = 3600;
        minimumTokenBalanceForDividends = 200000 * (10**18); //must hold 200000+ tokens
    }

    function _transfer(address, address, uint256) internal override {
        require(false, "BABYCAKE_Dividend_Tracker: No transfers allowed");
    }

    function withdrawDividend() public override {
        require(false, "BABYCAKE_Dividend_Tracker: withdrawDividend disabled. Use the 'claim' function on the main BABYCAKE contract.");
    }

    function excludeFromDividends(address account) external onlyOwner {
    	require(!excludedFromDividends[account]);
    	excludedFromDividends[account] = true;

    	_setBalance(account, 0);
    	tokenHoldersMap.remove(account);

    	emit ExcludeFromDividends(account);
    }

    function updateClaimWait(uint256 newClaimWait) external onlyOwner {
        require(newClaimWait >= 3600 && newClaimWait <= 86400, "BABYCAKE_Dividend_Tracker: claimWait must be updated to between 1 and 24 hours");
        require(newClaimWait != claimWait, "BABYCAKE_Dividend_Tracker: Cannot update claimWait to same value");
        emit ClaimWaitUpdated(newClaimWait, claimWait);
        claimWait = newClaimWait;
    }

    function getLastProcessedIndex() external view returns(uint256) {
    	return lastProcessedIndex;
    }

    function getNumberOfTokenHolders() external view returns(uint256) {
        return tokenHoldersMap.keys.length;
    }



    function getAccount(address _account)
        public view returns (
            address account,
            int256 index,
            int256 iterationsUntilProcessed,
            uint256 withdrawableDividends,
            uint256 totalDividends,
            uint256 lastClaimTime,
            uint256 nextClaimTime,
            uint256 secondsUntilAutoClaimAvailable) {
        account = _account;

        index = tokenHoldersMap.getIndexOfKey(account);

        iterationsUntilProcessed = -1;

        if(index >= 0) {
            if(uint256(index) > lastProcessedIndex) {
                iterationsUntilProcessed = index.sub(int256(lastProcessedIndex));
            }
            else {
                uint256 processesUntilEndOfArray = tokenHoldersMap.keys.length > lastProcessedIndex ?
                                                        tokenHoldersMap.keys.length.sub(lastProcessedIndex) :
                                                        0;


                iterationsUntilProcessed = index.add(int256(processesUntilEndOfArray));
            }
        }


        withdrawableDividends = withdrawableDividendOf(account);
        totalDividends = accumulativeDividendOf(account);

        lastClaimTime = lastClaimTimes[account];

        nextClaimTime = lastClaimTime > 0 ?
                                    lastClaimTime.add(claimWait) :
                                    0;

        secondsUntilAutoClaimAvailable = nextClaimTime > block.timestamp ?
                                                    nextClaimTime.sub(block.timestamp) :
                                                    0;
    }

    function getAccountAtIndex(uint256 index)
        public view returns (
            address,
            int256,
            int256,
            uint256,
            uint256,
            uint256,
            uint256,
            uint256) {
    	if(index >= tokenHoldersMap.size()) {
            return (0x0000000000000000000000000000000000000000, -1, -1, 0, 0, 0, 0, 0);
        }

        address account = tokenHoldersMap.getKeyAtIndex(index);

        return getAccount(account);
    }

    function canAutoClaim(uint256 lastClaimTime) private view returns (bool) {
    	if(lastClaimTime > block.timestamp)  {
    		return false;
    	}

    	return block.timestamp.sub(lastClaimTime) >= claimWait;
    }

    function setBalance(address payable account, uint256 newBalance) external onlyOwner {
    	if(excludedFromDividends[account]) {
    		return;
    	}

    	if(newBalance >= minimumTokenBalanceForDividends) {
            _setBalance(account, newBalance);
    		tokenHoldersMap.set(account, newBalance);
    	}
    	else {
            _setBalance(account, 0);
    		tokenHoldersMap.remove(account);
    	}

    	processAccount(account, true);
    }

    function process(uint256 gas) public returns (uint256, uint256, uint256) {
    	uint256 numberOfTokenHolders = tokenHoldersMap.keys.length;

    	if(numberOfTokenHolders == 0) {
    		return (0, 0, lastProcessedIndex);
    	}

    	uint256 _lastProcessedIndex = lastProcessedIndex;

    	uint256 gasUsed = 0;

    	uint256 gasLeft = gasleft();

    	uint256 iterations = 0;
    	uint256 claims = 0;

    	while(gasUsed < gas && iterations < numberOfTokenHolders) {
    		_lastProcessedIndex++;

    		if(_lastProcessedIndex >= tokenHoldersMap.keys.length) {
    			_lastProcessedIndex = 0;
    		}

    		address account = tokenHoldersMap.keys[_lastProcessedIndex];

    		if(canAutoClaim(lastClaimTimes[account])) {
    			if(processAccount(payable(account), true)) {
    				claims++;
    			}
    		}

    		iterations++;

    		uint256 newGasLeft = gasleft();

    		if(gasLeft > newGasLeft) {
    			gasUsed = gasUsed.add(gasLeft.sub(newGasLeft));
    		}

    		gasLeft = newGasLeft;
    	}

    	lastProcessedIndex = _lastProcessedIndex;

    	return (iterations, claims, lastProcessedIndex);
    }

    function processAccount(address payable account, bool automatic) public onlyOwner returns (bool) {
        uint256 amount = _withdrawDividendOfUser(account);

    	if(amount > 0) {
    		lastClaimTimes[account] = block.timestamp;
            emit Claim(account, amount, automatic);
    		return true;
    	}

    	return false;
    }
}
